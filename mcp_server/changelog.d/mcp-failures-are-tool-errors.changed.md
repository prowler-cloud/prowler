Tools now report a failure as an MCP tool execution error (`isError: true`, explanation in `content`) instead of as a successful result carrying an `{"error": ...}` object, which clients and models read as a success. `prowler_send_findings_to_jira` stays result-based: a dispatch creates work items one by one, so a failure halfway through has to report how far it got and whether sending the findings again is safe
