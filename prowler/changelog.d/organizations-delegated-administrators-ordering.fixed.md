`organizations_delegated_administrators` no longer reports PASS when an untrusted delegated administrator is followed by a trusted one, which made the verdict depend on the order `ListDelegatedAdministrators` returned, and now names the administrators in a sorted order and in the plural when there is more than one, so that one unchanged organization no longer produces two different findings across scans
