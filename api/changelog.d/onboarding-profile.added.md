`POST /onboarding-profiles` records the cloud-account count, team size, area of work and position a tenant declares at first login, or that the step was skipped, once per tenant and never overwritten
