# AI Elements

Components vendored from Vercel's AI Elements registry
(https://ai-sdk.dev/elements), adapted to this repo's shadcn imports and
lint rules. They live in their own folder — same idea as `components/shadcn/`
— so they can be diffed against upstream when updating instead of being mixed
with hand-written chat UI, which lives in `../chat/`.
